---
description: 대상 작업을 한 단계 분해한다.
argument-hint: [분해할 대상 작업 식별자 또는 설명 (선택)]
---

`${CLAUDE_PLUGIN_ROOT}/methods/how-to-divide.md` 를 읽고, 그 방법론대로 대상 작업을
한 단계 분해한다.

분해 결과를 장기메모리에 반영하는 것은 이 명령의 책임이 아니다 — 저장이 필요하면
사용자가 `update-memory` 를 명시적으로 호출한다.

대상: $ARGUMENTS
