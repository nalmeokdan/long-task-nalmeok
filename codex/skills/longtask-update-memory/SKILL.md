---
name: longtask-update-memory
description: 장기작업(longtask)에서 현재 세션 내용을 기반으로 장기메모리를 업데이트한다. 사용자가 update-memory·메모리 반영·저장을 명시적으로 요청할 때만 쓴다 — 시키지 않은 자동 적재는 하지 않는다.
---

이 스킬 디렉터리의 `references/how-to-manage-memory.md` 를 읽고, 그 규약대로 이번 세션
내용을 장기메모리에 반영해 갱신한다.

이번 세션 요약·반영 대상은 사용자가 이 스킬을 부르며 준 설명·맥락에서 취한다.
